<p id="notice"><%= notice %></p>

<h1>Listing Matches</h1>

<ul>
    <% @matches.each do |match| %>
        <li><%= link_to User.find(match.target_id).email, match %></li>
    <% end %>
</ul>

<br>

<%= link_to 'New Match', new_match_path %>
